apiVersion: 1

providers:
  - name: ChainMonitoring    # A uniquely identifiable name for the provider
    folder: Monitoring # The folder where to place the dashboards
    type: file
    options:
      path: /var/lib/grafana/dashboards
